# Environment for a locally served reasoning model.
# Copy to serving.env, fill in the values, and `source` it before run.sh.
export REEF_UPSTREAM_URL=http://127.0.0.1:30011
export REEF_MODEL=/models/GLM-5.3-Flash
export REEF_UPSTREAM_API_KEY=your-local-key
export REEF_SC_JUDGE_BASE=http://host.docker.internal:30011/v1
export REEF_SC_JUDGE_MODEL=/models/GLM-5.3-Flash
export REEF_PI_BINARY=pi
