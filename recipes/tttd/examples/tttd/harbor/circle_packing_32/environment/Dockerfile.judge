# The judge's container: the server plus this task's scoring files.
FROM python:3.12-slim

RUN pip install --no-cache-dir "numpy==1.26.4" scipy cvxpy

COPY judge_server.py score.py judge_config.json /judge/
CMD ["python3", "/judge/judge_server.py"]
