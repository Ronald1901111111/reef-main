[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "reef-gepa-aime-harness"
version = "0.1.0"
description = "AIME validation example for the Reef GEPA harness-evolution method"
requires-python = ">=3.12"
# The driver embeds the Reef service and runs Pi episodes itself, so it also
# imports reef; run.sh supplies it from the repo via PYTHONPATH. datasets is
# the pinned AIME loader. There is no gepa dependency: the method is Reef's.
dependencies = [
    "reef-client",
    "datasets",
    "pyyaml",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["harness*"]
